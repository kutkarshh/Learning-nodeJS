<!DOCTYPE html>
<html lang="en">

<head>
    <title>Blog - Blogosaurus</title>
    <%- include('partials/head') %>
</head>

<body>
    <%- include('partials/navbar.ejs') %>
        <%- include('partials/script.ejs') %>
            <div class="container mt-5">
                <div class="card card-body">
                    <% if (locals.blog) { %>
                        <h1 class="">
                            <%= blog.title %>
                        </h1>
                        <div class="m-2">
                            <img src="/images<%= blog.createdBy.profileImageURL%>" width="50px" height="50px"
                                class="img-fluid border border-secondary" />
                            <%= blog.createdBy.fullName %>

                        </div>
                        <div class="text-center my-2">
                            <img src="<%= blog.coverImageURL %>" width="100%" height="500px" class="text-center"
                                alt="coverImageURL">
                        </div>
                        <div>
                            <p class="text-justify">
                                <%= blog.body %>
                            </p>

                        </div>

                        <% } %>
                </div>
            </div>


</body>

</html>